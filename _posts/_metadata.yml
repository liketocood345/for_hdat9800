# Shared defaults for _posts/**/*.qmd when rendered via rmarkdown::render().
# Site-wide theme/nav come from _site.yml (theme.css + favicon.html).
# Per-post YAML should set: output: distill::distill_article: self_contained: false
