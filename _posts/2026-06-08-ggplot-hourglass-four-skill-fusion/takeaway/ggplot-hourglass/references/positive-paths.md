# Positive paths (four mode roots)

## explore — path_depth: long

```text
T0 profile(data) → T1 BIND(chart) → T3 render(leaf)
  stack=r      → ggplot-skills template section
  stack=python → rosetta Fig1/2/3
delivery: png-dpi-800
```

## manuscript — path_depth: medium

```text
T0 → T1 BIND → belt(scientific-plotting) → T3 render
leaf: ggplot-skills + scientific constraints
delivery: pdf-mm-85
```

## review — path_depth: short

```text
T4 → ggplot-skills § Figure Review Checklist
skip_bind: true (unless user asks to change geom)
```

## gallery — path_depth: longest

```text
T1 agentfiguregallery query --task "..."
T2 gallery --plot-type <lookup> --serve  → STOP until human select
T3 prefer → bundle → render from bundle
leaf: gallery-bundle
```

## Waist field order (explore spine example)

```text
mode:explore → data:threshold-categorical → chart:bar-template → delivery:png-dpi-800
```

FIELD_NEXT chain in the HTML viewer shows this spine.
